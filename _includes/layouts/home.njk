---
layout: layouts/base.njk
templateClass: home
---

{{ content | safe }}

{# Posts:

<ul>
{%- for post in collections.post -%}
  <li><a href="{{post.url}}">{{ post.data.title }}</a> (<time datetime="{{ post.date | htmlDateString }}">{{ post.date | monthYearDate }}</time>)</li>
{%- endfor -%}
</ul> #}